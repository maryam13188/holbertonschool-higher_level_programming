Module with class MyList

First, let's import the MyList class:
    >>> MyList = __import__('1-my_list').MyList

Now, let's instantiate MyList:
    >>> my_list = MyList()
    >>> print(my_list)
    []

Checking the inheritance from list:
    >>> isinstance(my_list, list)
    True

Adding elements to the list:
    >>> my_list.append(1)
    >>> my_list.append(4)
    >>> my_list.append(2)
    >>> my_list.append(3)
    >>> my_list.append(5)
    >>> print(my_list)
    [1, 4, 2, 3, 5]

Testing the print_sorted method:
    >>> my_list.print_sorted()
    [1, 2, 3, 4, 5]

Verifying the original list remains unchanged:
    >>> print(my_list)
    [1, 4, 2, 3, 5]

Testing with other integer values:
    >>> my_list = MyList([10, -5, 0, 1])
    >>> my_list.print_sorted()
    [-5, 0, 1, 10]
    >>> print(my_list)
    [10, -5, 0, 1]

Testing with an empty list:
    >>> my_list = MyList()
    >>> my_list.print_sorted()
    []
